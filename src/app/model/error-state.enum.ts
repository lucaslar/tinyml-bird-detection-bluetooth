/**
 * Enumeration of possible error states (translation keys).
 */
export enum ErrorState {
    API_DISABLED = 'bluetooth.errorState.apiDisabled',
    OTHER = 'bluetooth.errorState.other',
}

// "tryAgain": "Zum Neuverbinden drücken"
