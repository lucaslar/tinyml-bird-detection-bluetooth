import { BirdCharacteristic } from './bird-characteristic';

describe('BirdCharacteristic', () => {
  it('should create an instance', () => {
    expect(new BirdCharacteristic()).toBeTruthy();
  });
});
