import { SortByValuePipe } from './sort-by-value.pipe';

describe('SortByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByValuePipe();
    expect(pipe).toBeTruthy();
  });
});
