import { NewPageLinkDirective } from './new-page-link.directive';

describe('NewPageLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new NewPageLinkDirective();
    expect(directive).toBeTruthy();
  });
});
